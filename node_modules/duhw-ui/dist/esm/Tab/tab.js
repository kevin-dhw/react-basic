import React, { useState } from "react";
import * as Style from "./style";
const Tab = (props) => {
    const { tabsData } = props;
    const [num, setNum] = useState(0);
    const handleClick = (index) => {
        setNum(index);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Style.Tab, null, tabsData.map((item, index) => {
            return (React.createElement(Style.TabItem, { onClick: () => handleClick(index), key: index, "$bgColor": num === index ? "red" : "" },
                React.createElement(Style.Tab_Item, { "$color": num === index ? "white" : "" }, item.label)));
        }))));
};
export default Tab;
