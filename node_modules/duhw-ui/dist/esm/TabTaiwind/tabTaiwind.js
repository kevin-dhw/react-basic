import React, { useState } from "react";
import classNames from "classnames";
const TabTaiWind = (props) => {
    const { tabsData } = props;
    const [num, setNum] = useState(0);
    const handleClick = (index) => {
        console.log(111);
        setNum(index);
    };
    return (React.createElement("div", { className: " flex" }, tabsData === null || tabsData === void 0 ? void 0 : tabsData.map((item, index) => {
        return (React.createElement("div", { onClick: () => handleClick(index), className: classNames(" mr-5  px-6", index === num ? "bg-red-500 text-white" : ""), key: index }, item.label));
    })));
};
export default TabTaiWind;
